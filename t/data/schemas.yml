components:
  schemas:
    Common:
      type: object
      properties:
        environment:
          $ref: "#/components/schemas/Environment"
        user:
          type: string
          maxLength: 255
      required:
        - user
    DeploymentStatus:
      type: object
      properties:
        status:
          $ref: "#/components/schemas/DeploymentStatusConstant"
        message:
          type: string
      required:
        - status
    Basic:
      type: string
    Nullable:
      type: integer
      nullable: true
    SortOrder:
      type: string
      enum:
        - asc
        - desc
    Environment:
      type: string
      nullable: true
      enum:
        - dev
        - test
        - prod
        - null
    DeploymentStatusConstant:
      type: string
      enum:
        - triggered
        - pending
        - succeeded
        - service_failed
        - failed
    Password:
      type: string
      minLength: 8
    Fullname:
      type: string
      minLength: 1
      maxLength: 255
    Initials:
      type: string
      maxLength: 3
